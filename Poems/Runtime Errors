/**
 * I program my life as a computer,
 * and feel conflicted every time
 * human limitations cause runtime errors
 * crashing meticulous instructions
 * from my past self.
 */

WakeUp(too_early) {
  SetErrorMessage(eyes_still_closed);
}

GetUp(too_early) {
  WaitUntil(panic_pushes_me_out_of_bed);
}

MundaneMorningTasks() {
  Completed (in_a_rush OR apathetically_dawdling_with_no_motivation_to_get_out_the_door);
}

GoToClass() {
  Arrive (a_few_minutes_late_and_sweaty_from_the_walk OR not_at_all_because_I_can't_bare_the_thought_of_spending_so_many_eternities_in_boredom_watching_my_life_tick_away);
}
